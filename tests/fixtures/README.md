Capability Tags used only by the test suite.

Valid tags live in `examples/` and are loaded from there — a fixture that drifts from the
shipped example stops testing what ships. This directory holds only tags that exist to be
rejected, or that exercise a shape no shipped example covers.
